/**
 * @file
 * Acten Blog Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.actenBlogTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
